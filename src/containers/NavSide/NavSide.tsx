import React from "react";
import styles from "./NavSide.module.scss";

const NavSide = () => {
  return (
    <aside>
      <nav className={styles.container}>네브 사이드</nav>
    </aside>
  );
};

export default NavSide;
