import React from 'react';
import styles from './Footer.module.scss';

// footer 레이아웃 컴포넌트입니다.
const Footer = () => {
  return <footer className={styles.container}>푸터</footer>;
};

export default Footer;
